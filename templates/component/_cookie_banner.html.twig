{% if app.session.get('cookies_accepted') is null %}
<div id="cookie-banner" class="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 shadow-lg z-50 px-6 py-4">
    <div class="max-w-4xl mx-auto flex flex-col sm:flex-row items-center justify-between gap-4">
        <p class="text-gray-700 text-sm leading-relaxed">
            🍪 Ce site utilise des cookies techniques nécessaires à son fonctionnement (session, panier). 
            <a href="{{ path('app_confidentialite') }}" class="text-chocolat underline">En savoir plus</a>
        </p>
        <div class="flex gap-3 shrink-0">
            <button onclick="acceptCookies()" class="bg-chocolat text-white px-5 py-2 rounded-full text-sm font-semibold hover:opacity-90 transition">
                Accepter
            </button>
            <button onclick="refuseCookies()" class="border border-gray-400 text-gray-600 px-5 py-2 rounded-full text-sm hover:bg-gray-100 transition">
                Refuser
            </button>
        </div>
    </div>
</div>

<script>
function acceptCookies() {
    fetch('/cookies/accept', { method: 'POST' })
        .then(() => document.getElementById('cookie-banner').remove());
}
function refuseCookies() {
    fetch('/cookies/refuse', { method: 'POST' })
        .then(() => document.getElementById('cookie-banner').remove());
}
</script>
{% endif %}