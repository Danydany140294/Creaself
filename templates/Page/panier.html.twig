{% extends 'base.html.twig' %}

{% block title %}Mon Panier - CreaSelf{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/panier.css') }}">
{% endblock %}

{% block body %}
<main class="cart-experience">
    <div class="cart-container">
        <!-- En-tête de page -->
        <header class="cart-header">
            <h1 class="fade-in">
                Votre <span class="pink-neon-text italic">Sélection</span>
            </h1>
            <p class="cart-subtitle">
                {{ panier.nombre_articles ?? 0 }} Trésors prêts à être dégustés
            </p>
        </header>

        {% if panier.is_empty is defined and panier.is_empty %}
            <!-- Panier vide -->
            <div class="empty-cart glass-premium">
                <i class="fa-solid fa-basket-shopping empty-icon"></i>
                <h2>Votre panier est vide</h2>
                <p>Découvrez nos délicieux cookies et boxes gourmandes !</p>
                <a href="{{ path('app_home') }}" class="btn-back-shop">
                    <i class="fas fa-arrow-left"></i>
                    Retour à la boutique
                </a>
            </div>
        {% else %}
            <!-- Panier avec articles -->
            <div class="cart-layout">
                <!-- Liste des produits -->
                <section class="cart-items-wrapper">
                    
                    {# ========== PANIER BDD (Utilisateur connecté) ========== #}
                    {% if panier.lignes is defined %}
    {% for ligne in panier.lignes %}
        <div class="cart-row glass-premium" data-id="{{ ligne.id }}" data-type="{{ ligne.type }}">

            <div class="product-preview">
                <div class="img-glow-wrap">
                    {% if ligne.image %}
                        {% if ligne.type == 'Cookie' %}
                            <img src="{{ asset('asset/images/produits/' ~ ligne.image) }}"
                                 alt="{{ ligne.nom }}"
                                 onerror="this.src='https://via.placeholder.com/150/FF6B8F/FFFFFF?text=Cookie'">
                        {% else %}
                            <img src="{{ asset('asset/images/boxs/' ~ ligne.image) }}"
                                 alt="{{ ligne.nom }}"
                                 onerror="this.src='https://via.placeholder.com/150/FF6B8F/FFFFFF?text=Box'">
                        {% endif %}
                    {% else %}
                        <img src="https://via.placeholder.com/150/FF6B8F/FFFFFF?text=Article" alt="{{ ligne.nom }}">
                    {% endif %}
                </div>
            </div>

            <div class="product-details">
                <span class="product-category">{{ ligne.type|upper }}</span>
                <h3>{{ ligne.nom }}</h3>

                {% if ligne.composition is defined and ligne.composition is not empty %}
                    <div class="box-composition">
                        <p class="flavor-notes">📦 Composition :</p>
                        <div class="composition-list">
                            {% for compo in ligne.composition %}
                                <span class="composition-item">{{ compo.quantite }}x {{ compo.nom }}</span>
                            {% endfor %}
                        </div>
                    </div>
                {% else %}
                    <p class="flavor-notes">Prix unitaire : {{ ligne.prix_unitaire|number_format(2, ',', ' ') }} €</p>
                {% endif %}
            </div>

            <div class="product-qty">
                {% if ligne.type != 'Box Personnalisée' and not (ligne.type starts with 'Box Personnalisée') %}
                    <div class="qty-stepper">
                        <button onclick="changeQtyDB({{ ligne.id }}, '{{ ligne.type }}', -1)">−</button>
                        <input type="text" value="{{ ligne.quantite }}" readonly>
                        <button onclick="changeQtyDB({{ ligne.id }}, '{{ ligne.type }}', 1)">+</button>
                    </div>
                {% else %}
                    <div class="qty-display">Quantité : {{ ligne.quantite }}</div>
                {% endif %}
            </div>

            <div class="product-price">
                <span class="total-item-price">{{ ligne.sous_total|number_format(2, ',', ' ') }} €</span>
                <button class="btn-remove" onclick="removeItemDB({{ ligne.id }}, '{{ ligne.type }}')">
                    SUPPRIMER
                </button>
            </div>
        </div>
    {% endfor %}
{% endif %}

                    {# ========== PANIER SESSION (Visiteur) - PRODUITS ========== #}
                    {% if panier.produits is defined %}
                        {% for item in panier.produits %}
                            <div class="cart-row glass-premium" data-id="{{ item.produit.id }}" data-type="Cookie">
                                
                                <div class="product-preview">
                                    <div class="img-glow-wrap">
                                        <img src="{{ asset('asset/images/produits/' ~ item.produit.image) }}" 
                                             alt="{{ item.produit.name }}"
                                             onerror="this.src='https://via.placeholder.com/150/FF6B8F/FFFFFF?text=Cookie'">
                                    </div>
                                </div>

                                <div class="product-details">
                                    <span class="product-category">COOKIE ARTISANAL</span>
                                    <h3>{{ item.produit.name }}</h3>
                                    <p class="flavor-notes">Prix unitaire : {{ item.produit.prix|number_format(2, ',', ' ') }} €</p>
                                </div>

                                <div class="product-qty">
                                    <div class="qty-stepper">
                                        <button onclick="changeQtySession({{ item.produit.id }}, 'Cookie', -1)">−</button>
                                        <input type="text" value="{{ item.quantite }}" readonly>
                                        <button onclick="changeQtySession({{ item.produit.id }}, 'Cookie', 1)">+</button>
                                    </div>
                                </div>

                                <div class="product-price">
                                    <span class="total-item-price">{{ (item.produit.prix * item.quantite)|number_format(2, ',', ' ') }} €</span>
                                    <button class="btn-remove" onclick="removeItemSession({{ item.produit.id }}, 'Cookie')">
                                        SUPPRIMER
                                    </button>
                                </div>
                            </div>
                        {% endfor %}
                    {% endif %}

                    {# ========== PANIER SESSION - BOXES FIXES ========== #}
                    {% if panier.boxes is defined %}
                        {% for item in panier.boxes %}
                            <div class="cart-row glass-premium" data-id="{{ item.box.id }}" data-type="Box">
                                
                                <div class="product-preview">
                                    <div class="img-glow-wrap">
                                        <img src="{{ asset('asset/images/boxs/' ~ item.box.image) }}" 
                                             alt="{{ item.box.nom }}"
                                             onerror="this.src='https://via.placeholder.com/150/FF6B8F/FFFFFF?text=Box'">
                                    </div>
                                </div>

                                <div class="product-details">
                                    <span class="product-category">BOX {{ item.box.type|upper }}</span>
                                    <h3>{{ item.box.nom }}</h3>
                                    <p class="flavor-notes">Prix unitaire : {{ item.box.prix|number_format(2, ',', ' ') }} €</p>
                                </div>

                                <div class="product-qty">
                                    <div class="qty-stepper">
                                        <button onclick="changeQtySession({{ item.box.id }}, 'Box', -1)">−</button>
                                        <input type="text" value="{{ item.quantite }}" readonly>
                                        <button onclick="changeQtySession({{ item.box.id }}, 'Box', 1)">+</button>
                                    </div>
                                </div>

                                <div class="product-price">
                                    <span class="total-item-price">{{ (item.box.prix * item.quantite)|number_format(2, ',', ' ') }} €</span>
                                    <button class="btn-remove" onclick="removeItemSession({{ item.box.id }}, 'Box')">
                                        SUPPRIMER
                                    </button>
                                </div>
                            </div>
                        {% endfor %}
                    {% endif %}

                    
                   {# ========== PANIER SESSION - BOXES PERSO ========== #}
{% if panier.boxes_perso is defined %}
    {% for key, item in panier.boxes_perso %}
        <div class="cart-row glass-premium" data-id="{{ key }}" data-type="BoxPerso">
            
            <div class="product-preview">
                <div class="img-glow-wrap">
                    <img src="{{ asset('asset/images/boxs/' ~ item.box.image) }}" 
                         alt="Box Personnalisée"
                         onerror="this.src='https://via.placeholder.com/150/FF6B8F/FFFFFF?text=Box'">
                </div>
            </div>

            <div class="product-details">
                <span class="product-category">BOX PERSONNALISÉE</span>
                <h3>Ma Box Sur-Mesure — {{ item.taille }} cookies</h3>
                <div class="box-composition">
                    <p class="flavor-notes">📦 Composition :</p>
                    <div class="composition-list">
                        {% for compo in item.compositions %}
                            <span class="composition-item">
                                {{ compo.quantite }}x {{ compo.produit.name }}
                            </span>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <div class="product-qty">
                <div class="qty-display">
                    Quantité : 1
                </div>
            </div>

            <div class="product-price">
                <span class="total-item-price">{{ item.prix|number_format(2, ',', ' ') }} €</span>
                <button class="btn-remove" onclick="removeItemSession({{ key }}, 'BoxPerso')">
                    SUPPRIMER
                </button>
            </div>
        </div>
    {% endfor %}
{% endif %}

                </section>

                <!-- Sidebar de résumé -->
                <aside class="cart-sidebar">
                    <div class="summary-card glass-premium sticky-summary">
                        <h2>Résumé de la <span>Commande</span></h2>

                        <!-- Barre de progression pour livraison gratuite -->
                        <div class="shipping-info">
                            {% set seuil_gratuit = 50 %}
                            {% set reste = seuil_gratuit - panier.total %}
                            {% if reste > 0 %}
                                <p>Livraison offerte : <span class="remaining">Plus que {{ reste|number_format(2, ',', ' ') }} €</span></p>
                                <div class="progress-container">
                                    <div class="progress-bar-fill" style="width: {{ (panier.total / seuil_gratuit * 100)|round }}%;"></div>
                                </div>
                            {% else %}
                                <p class="free-shipping">🎉 Livraison offerte !</p>
                            {% endif %}
                        </div>

                        <!-- Détails du résumé -->
                        <div class="summary-details">
                            <div class="detail-line">
                                <span>Sous-total</span>
                                <span>{{ panier.total|number_format(2, ',', ' ') }} €</span>
                            </div>
                            <div class="detail-line">
                                <span>Livraison</span>
                                <span>{{ reste > 0 ? '4,50 €' : 'Gratuite' }}</span>
                            </div>
                            <div class="detail-line total">
                                <span>TOTAL</span>
                                <span class="final-amount">
                                    {{ (reste > 0 ? panier.total + 4.50 : panier.total)|number_format(2, ',', ' ') }} €
                                </span>
                            </div>
                        </div>

                        <!-- Bouton de commande -->
                        <a href="{{ path('app_paiement') }}" class="btn-checkout-neon" style="display:block; text-align:center; text-decoration:none;">
    PASSER AU PAIEMENT
    <span class="btn-sparkle">✨</span>
</a>

                        <!-- Bouton vider le panier -->
                        <form method="POST" action="{{ path('app_panier_vider') }}" onsubmit="return confirm('Vider tout le panier ?')" style="margin-top: 15px;">
                            <button type="submit" class="btn-clear-cart">
                                <i class="fas fa-trash-alt"></i> Vider le panier
                            </button>
                        </form>

                        <!-- Trust badges -->
                        <div class="trust-badges">
                            <span>🛡️ Paiement 100% Sécurisé</span>
                            <span>🚀 Expédition sous 24h</span>
                        </div>
                    </div>
                </aside>
            </div>
        {% endif %}
    </div>
</main>
{% endblock %}

{% block javascripts %}
      {{ parent() }}
    <script src="https://js.stripe.com/v3/"></script>
    <script>
        // Passer la clé publique Stripe au JavaScript
        const STRIPE_PUBLIC_KEY = '{{ stripe_public_key }}';
    </script>
    <script src="{{ asset('js/panier.js') }}"></script>
{% endblock %}