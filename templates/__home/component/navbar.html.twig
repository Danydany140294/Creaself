{# templates/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}CreaSelf{% endblock %}</title>
    
    {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/navbar.css') }}">
    {% endblock %}

   

</head>
<body>


        <header>
            <nav class="navbar">
                <!-- Logo à gauche -->
                <div class="logo">
                    <a href="{{ path('app_home') }}">
                        <img src="{{ asset('css/images/Logo.png') }}" alt="Logo">
                    </a>
                </div>

                <!-- Menu de navigation -->
                <ul class="nav-links">
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li><a href="{{ path('app_atelier') }}">Ateliers</a></li>
                    <li><a href="{{ path('app_box') }}">Nos Box</a></li>
                    {# <li><a href="{{ path('app_contact') }}">Contact</a></li> #}
                </ul>

                <!-- Barre de recherche -->
                {# <form action="{{ path('app_recherche') }}" method="get" class="search-bar">
                    <input type="text" name="q" placeholder="Rechercher...">
                    <button type="submit">🔍</button>
                </form> #}

                <!-- Boutons à droite -->
                <div class="nav-buttons">
                    <a href="{{ path('app_login') }}" class="btn">Login</a>
                    <a href="{{ path('app_panier') }}" class="btn">Panier</a>
                </div>

                <!-- Bouton hamburger pour mobile -->
                <div class="hamburger" onclick="toggleMenu()">☰</div>
            </nav>
        </header>
       
    {% block javascripts %}
    <script src="{{ asset('public\js\main.js') }}"></script>
    <script>
    function toggleMenu() {
        const navbar = document.querySelector('.navbar');
        navbar.classList.toggle('expanded');
    }
    </script>

    {% endblock %}
</body>
</html>