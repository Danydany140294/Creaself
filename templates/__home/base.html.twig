{# templates/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}CreaSelf{% endblock %}</title>
    
    {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/base.css') }}">
    <link rel="stylesheet" href="{{ asset('css/navbar.css') }}">
    {% endblock %}

   

</head>

    {% include '__home/component/navbar.html.twig' %}

     

       
            {% block body %}
            <body class="intro-active">
    
    {# Animation d'intro du logo #}
    <div id="logo-intro">
                <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="sparkle"></div>
        <div class="sparkle"></div>
        <div class="sparkle"></div>
        <div class="sparkle"></div>
        <div class="sparkle"></div>
        <div class="sparkle"></div>
        <div class="sparkle"></div>
        <div class="sparkle"></div>
        
        <div class="logo-container">
            <img src="{{ asset('asset/logo1.jpg') }}" alt="CreaSelf Logo">
        </div>
    </div>
            {% endblock %}
        


    {% include '__home/component/footer.html.twig' %}

    
       
    {% block javascripts %}
    {# <script src="{{ asset('public\js\main.js') }}"></script> #}

      <script>
        // Gestion de l'animation d'intro
        document.addEventListener('DOMContentLoaded', function() {
            const logoIntro = document.getElementById('logo-intro');
            const body = document.body;

            // Après 4 secondes, on cache l'intro
            setTimeout(() => {
                logoIntro.classList.add('hidden');
                body.classList.remove('intro-active');
                
                // Supprime complètement l'élément après l'animation
                setTimeout(() => {
                    logoIntro.remove();
                }, 800);
            }, 6000);
        });
        </script>
    
    {% endblock %}
</body>
</html>