<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe oublié - LOGIX</title>
    <link rel="stylesheet" href="{{ asset('css/login.css') }}">
</head>
<body>
    <a href="{{ path('app_login') }}" class="back-btn">BACK</a>
    
    <div class="container">
        <div class="floating-elements">
            <div></div>
            <div></div>
            <div></div>
            <div></div>
            <div></div>
        </div>
        <div class="form">
            <div class="logo">
    <a href="{{ path('app_home') }}">
        <img src="{{ asset('asset/perline.jpg') }}" alt="Logo" style="max-width: 150px; height: auto; border-radius: 10px;">
    </a>
</div>
            
            <p style="color: white; text-align: center; margin-bottom: 30px; font-size: 0.9em;">
                Entrez votre email pour recevoir un lien de réinitialisation
            </p>
            
            {{ form_start(requestForm) }}
                {% for flash_error in app.flashes('reset_password_error') %}
                    <div class="alert">{{ flash_error }}</div>
                {% endfor %}

                <div class="inputBox">
                    {{ form_widget(requestForm.email, {'attr': {'required': 'required'}}) }}
                    <span>Email</span>
                </div>

                <button type="submit" class="enter">ENVOYER</button>
                
                <div class="links" style="justify-content: center;">
                    <a href="{{ path('app_login') }}">← Retour à la connexion</a>
                </div>
            {{ form_end(requestForm) }}
        </div>
    </div>
</body>
</html>